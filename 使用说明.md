# 静时

双击同目录的「静时.app」即可使用，不需要联网或安装依赖。

- 默认专注 25 分钟、短休息 5 分钟、长休息 15 分钟。
- 点击开始、暂停或继续；右侧按钮重新计时。
- 默认悬浮置顶，切换应用、桌面和全屏页面时仍保持显示；右上角图钉可以临时取消置顶，重新打开应用时默认恢复置顶。设置里可以调整时长、提示音和窗口不透明度（默认 70%，只让背景变透，数字和按钮保持清晰）。
- 每完成 4 次专注，下一段休息建议为长休息；下一段需手动开始。
- 关闭窗口后菜单栏继续显示倒计时；点击菜单栏可以重新打开或彻底退出。
- 时长、任务文字、窗口不透明度、计时进度和今日完成次数保存在本机。
- 退出后重新打开会按原定结束时间恢复；电脑睡眠或应用退出时不会响铃，重新打开或唤醒后更新状态。

源码：main.swift。重新编译：

```sh
cd /Users/penggao/Project/QuietTimer
xcrun swiftc main.swift -o 静时.app/Contents/MacOS/QuietTimer -framework SwiftUI -framework AppKit
codesign --force --sign - 静时.app
```

已验证：默认时长、暂停恢复、模式切换、时长调整、运行状态恢复、到时完成、避免重复计数、下一段切换。已打开原生窗口检查实际排版。

## 紧凑悬浮模式

- 开启图钉置顶后，窗口缩为 224 × 184 的小浮窗，突出倒计时。
- 小浮窗保留设置、取消置顶、暂停/继续和重置，细进度条替代大圆环。
- 取消置顶恢复完整界面，可编辑任务和切换计时模式；切换界面不会重置倒计时。
- 拖动窗口空白区域可以移动位置，原有透明度设置继续有效。
- 已实际验证从小浮窗展开、重新置顶缩小，计时连续且完整界面不裁切。

## 自由输入倒计时

点击大号时间或其下方的「设置时长」，填写分钟与秒，再点击「开始倒计时」。支持 1 秒至 999 分 59 秒；无效输入会提示且禁止开始。运行或暂停中修改时长时，按钮会显示「替换并开始」，取消编辑不会改变原计时。

自定义时长适用于当前模式，重置会回到该时长，重开应用也会保留；切换专注/休息模式或进入下一段时恢复该模式的预设时长。小浮窗和完整窗口均可直接编辑时间。

已验证输入边界、开始、暂停、重置、进度恢复、预设切换、到时完成，并实际操作输入 10 秒启动倒计时。
